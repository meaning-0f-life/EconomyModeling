# Моделирование экономики стран с использованием производственной функции Кобба-Дугласа и метода динамического программирования

[![Python](https://img.shields.io/badge/Python-3.8%2B-blue.svg)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](https://opensource.org/licenses/MIT)
[![Status](https://img.shields.io/badge/Status-Development-yellow.svg)]()

## 📋 Описание проекта

Данный проект реализует серию экономических моделей для анализа и прогнозирования развития стран с использованием метода динамического программирования Беллмана:

1. **Базовая модель** (`Economic.py`) - учебная модель для демонстрации метода динамического программирования в упрощенном экономическом контексте
2. **Основная модель с удельным капиталом** (`EuroEconomic.py`) - полнофункциональная модель, использующая удельные показатели (на одного рабочего) и реальные экономические данные Всемирного банка
3. **Прототип расширенной модели** (`EuroEconomicWithEmployment.py`) - экспериментальная модель для исследования возможностей расширения базового подхода

## 🎯 Цели проекта

- Анализ производственных функций для различных стран с использованием реальных данных
- Оптимизация инвестиционной стратегии с учетом инфляции и износа капитала
- Прогнозирование динамики удельного капитала и удельного ВВП
- Сравнение фактических экономических показателей с оптимальными траекториями
- Разработка прототипов для расширения модели на случай многофакторной производственной функции

## 📁 Структура проекта

```
EconomicProject/
├── Economic.py                      # Базовая модель (простая)
├── EuroEconomic.py                  # Модель с капиталом (Кобба-Дугласа: Y = A*K^α)
├── EuroEconomicWithEmployment.py    # Модель с капиталом и трудом (Y = A*K^α*L^β)
├── data/                            # Данные по экономическим показателям
│   ├── CapitalData.csv             # Данные по капиталу
│   ├── GDP_Data.csv                # Данные по ВВП
│   ├── InflationData.csv           # Данные по инфляции
│   ├── Employment.csv              # Данные по занятости
│   └── Population.csv              # Данные по населению
├── images/                          # Сгенерированные графики и визуализации
│   ├── <country>_<start>_<end>_analysis.png
│   ├── <country>_<start>_<end>_analysis_cobb_douglas.png
│   └── <country>_<start>_<end>_production_function_3d.png
└── README.md                        # Данный файл
```

## 🔬 Математическое описание моделей

### Модель 1: Базовая модель (Economic.py)

**Производственная функция:**
```
f(k) = A * (k^α)
```

**Динамика системы:**
```
k_{t+1} = (1 - λ) * k_t + p_t * f(k_t)
```

**Задача оптимизации:**
```
max Σ [u(p_t, f(k_t)) / (1 + γ)^t] + F_N(k_N)
```

где:
- `k_t` - капитал в момент времени t
- `p_t` - доля продукта, направляемая на инвестиции (управление)
- `λ` - коэффициент износа (0.05)
- `γ` - коэффициент инфляции (0.01)
- `u(c) = ln(c)` - функция полезности
- `F_N(k)` - терминальная функция

### Модель 2: Кобба-Дугласа с капиталом (EuroEconomic.py)

**Производственная функция:**
```
Y = A * K^α
ln(Y) = ln(A) + α * ln(K)
```

**Параметры оцениваются методом линейной регрессии:**
```
α = slope (эластичность по капиталу)
A = exp(intercept) (технологический параметр)
```

**Динамика системы:**
```
K_{t+1} = (1 - λ) * K_t + p_t * Y_t
```

**Особенности:**
- Учет инфляции за период
- Терминальная функция: `F = k_term * K + b_term`
- Начальное условие: реальные данные удельного капитала для выбранного года

### Модель 3: Кобба-Дугласа с капиталом и трудом (EuroEconomicWithEmployment.py)

**Производственная функция:**
```
Y = A * K^α * L^β
ln(Y) = ln(A) + α * ln(K) + β * ln(L)
```

**Параметры оцениваются методом множественной регрессии:**
```
A = exp(intercept)
α (эластичность по капиталу)
β (эластичность по труду)
Σ = α + β (отдача от масштаба)
```

**Особенности:**
- Учет динамики рабочей силы (население × занятость)
- Многомерная оптимизация
- 3D визуализация производственной функции

## 📊 Используемые данные

Все данные взяты из [World Bank Open Data](https://data.worldbank.org/):

| Показатель | Источник | Период |
|------------|----------|--------|
| ВВП (номинальный) | NY.GDP.MKTP.CD | 1995-2024 |
| Валовое накопление капитала | NE.GDI.FTOT.CD | 1995-2024 |
| Инфляция (дефлятор ВВП) | NY.GDP.DEFL.KD.ZG | 1995-2024 |
| Население | SP.POP.TOTL | 1995-2024 |
| Уровень занятости | SL.EMP.TOTL.SP.ZS | 1995-2024 |

**Страны для анализа:**
- Китай (CHN)
- Россия (RUS)
- ОАЭ (ARE)
- Армения (ARM)
- Беларусь (BLR)
- Бразилия (BRA)
- Канада (CAN)
- Германия (DEU)
- Индия (IND)
- Япония (JPN)
- Казахстан (KAZ)
- США (USA)

## ⚙️ Установка и запуск

### Требования

```bash
Python >= 3.8
numpy >= 1.21.0
pandas >= 1.3.0
matplotlib >= 3.4.0
scipy >= 1.7.0
```

### Установка зависимостей

```bash
pip install numpy pandas matplotlib scipy
```

### Запуск моделей

**Базовая модель:**
```bash
python Economic.py
```

**Модель с капиталом:**
```bash
python EuroEconomic.py
```

**Модель с капиталом и трудом (прототип):**
```bash
python EuroEconomicWithEmployment.py
```

### Пример использования

В каждом файле есть тестовый запуск в конце. Для запуска модели для конкретной страны:

```python
# Для EuroEconomic.py или EuroEconomicWithEmployment.py
run_model_for_country('Индия', start_year=1996, end_year=2001)
```

**Параметры:**
- `country_name` - название страны на русском
- `start_year` - начальный год (от 1995 до 2013)
- `end_year` - конечный год (до 2013)

## 🔢 Алгоритм решения

### Этап 1: Обратный ход Беллмана

```
Для i от N-2 до 0:
    Для каждого состояния k_j:
        F_i(k_j) = max_{p∈[0,1]} { u(p, f(k_j)) / (1+γ_i)^i + F_{i+1}(k_{next}) }
        s_i(k_j) = argmax_{p} { ... }
```

где:
- `F_i(k)` - функция Беллмана в момент i при состоянии k
- `s_i(k)` - оптимальное управление
- `k_{next}` = (1-λ)k + p·f(k)

### Этап 2: Прямой ход

```
k_0 = k_initial
p_0 = s_0(k_0)

Для i от 1 до N-1:
    k_i = (1-λ) * k_{i-1} + p_{i-1} * f(k_{i-1})
    p_i = s_i(k_i)
```

## 📈 Визуализация

Модели генерируют следующие графики:

1. **Сравнение капиталов** - фактический vs оптимальный
2. **Оптимальное управление** - доля инвестиций и инфляция
3. **Рабочая сила** - динамика (только для модели с трудом)
4. **Сравнение ВВП** - фактический vs модельный
5. **Производственная функция** - график Y(K) или 3D Y(K,L)
6. **Статистика модели** - параметры и ошибки

## 📉 Оценка качества модели

**MAPE (Mean Absolute Percentage Error):**
```
MAPE = (1/n) * Σ |(Y_actual - Y_model) / Y_actual| * 100%
```

**R² (коэффициент детерминации):**
```
R² = 1 - SSR / SST
```

**Темпы роста:**
```
Growth = (Y_end - Y_start) / Y_start * 100%
```

## 🔍 Примеры результатов

### Индия (1996-2001)

**Параметры производственной функции:**
- A = 0.1245
- α = 0.352 (эластичность по капиталу)
- β = 0.721 (эластичность по труду)
- α+β = 1.073 (возрастающая отдача от масштаба)
- R² = 0.945

**Результаты оптимизации:**
- Средняя доля инвестиций: 0.423
- Рост капитала: 38.2%
- Ошибка модели ВВП: 6.8%

## ⚠️ Важные замечания

1. **Горизонт планирования**: N = end_year - start_year + 1
2. **Начальное состояние**: Реальные данные капитала для start_year
3. **Инфляция**: Учитывается динамически для каждого года
4. **Рабочая сила**: Используется фиксированная траектория из реальных данных
5. **Оптимальное управление**: Доля инвестиций p∈[0, 1]

## 📚 Теоретическая основа

### Производственная функция Кобба-Дугласа

Общий вид:
```
Y = A * ∏ X_i^α_i
```

Для двух факторов:
```
Y = A * K^α * L^β
```

**Свойства:**
- Постоянная отдача от масштаба: α + β = 1
- Возрастающая отдача: α + β > 1
- Убывающая отдача: α + β < 1

**Эластичности:**
- По капиталу: ∂lnY/∂lnK = α
- По труду: ∂lnY/∂lnL = β

### Метод динамического программирования

**Уравнение Беллмана:**
```
V(k_t, t) = max_{p_t} { u(c_t) + δ * V(k_{t+1}, t+1) }
```

где:
- `V(k,t)` - функция ценности
- `δ` - коэффициент дисконтирования
- `c_t = (1-p_t) * f(k_t)` - потребление

## 🚀 Возможные улучшения

- [ ] Добавление дополнительных факторов производства (технологии, человеческий капитал)
- [ ] Моделирование внешнеторгового баланса
- [ ] Учет государственного регулирования
- [ ] Байесовские методы оценки параметров
- [ ] Стохастическая версия модели
- [ ] Интерактивный интерфейс (Streamlit/Dash)

## 📖 Ссылки

1. Пшеничников С.Н. Моделирование производственных функций для анализа динамики ВВП // Современные подходы к трансформации концепций государственного регулирования и управления в социально-экономических системах. – 2019. – С. 62-72.

2. Шнурков П.В., Рудак А.О. Алгоритмическое решение проблемы оптимального управления в динамической односекторной экономической модели с дискретным временем на основе метода динамического программирования // Системы и средства информатики. – 2019. – Т. 29. – № 1. – С. 128-139.

3. [World Bank Open Data](https://data.worldbank.org/)

4. [Kobb-Douglas Production Function](https://en.wikipedia.org/wiki/Cobb–Douglas_production_function)

## 📄 Лицензия

MIT License - свободное использование для научных и образовательных целей.

Разработано для моделирования экономического развития стран с использованием современных эконометрических методов.

---
**Версия**: 1.0.0  
**Последнее обновление**: 2026
